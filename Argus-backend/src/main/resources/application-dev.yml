project:
  name: Argus-backend
  version: 1.0.0

server:
  port: 10001

spring:
  application:
    name: argus-service
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:dd_rag}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:lgl}
    driver-class-name: org.postgresql.Driver

  # ──────────────────────────────────────────────
  # Spring AI Alibaba — DashScope 原生配置
  # ──────────────────────────────────────────────
  ai:
    model:
      chat: dashscope
      embedding: openai
    dashscope:
      api-key: ${DASHSCOPE_API_KEY:sk-ws-H.EPHEEEM.V532.MEYCIQCrE7JJ4fB-W-C8ZdiVx4xrVlGucZJ0KrgVc8wql6PweAIhAIXAWRX2SYBXB2yIvORX6njC93PsHa-pwRODkWA2BxqN}
      chat:
        options:
          model: qwen3.7-max
    openai:
      api-key: ${OPENAI_API_KEY:sk-ws-H.EPHEEEM.V532.MEYCIQCrE7JJ4fB-W-C8ZdiVx4xrVlGucZJ0KrgVc8wql6PweAIhAIXAWRX2SYBXB2yIvORX6njC93PsHa-pwRODkWA2BxqN}
      base-url: ${OPENAI_BASE_URL:https://dashscope.aliyuncs.com/compatible-mode}
      embedding:
        options:
          # 指定维度，确保与数据库表结构一致
          dimensions: 512
          model: qwen3.7-text-embedding
          # 单个输入超过模型 context length 时由 Ollama 截断，避免上传链路直接失败。
          truncate: true

    # 下面是 Ollama 相关配置，使用的话需要自己部署ollama对应的向量模型，然后放开spring-ai-starter-model-ollama，注释掉spring-ai-starter-model-openai
#    model:
#      chat: dashscope
#      embedding: ollama
#    dashscope:
#      api-key: ${DASHSCOPE_API_KEY:}
#      chat:
#        options:
#          model: glm-5
#    ollama:
#      base-url: http://localhost:11434
#      embedding:
#        options:
#          model: qllama/bge-small-zh-v1.5
#          # 单个输入超过模型 context length 时由 Ollama 截断，避免上传链路直接失败。
#          truncate: true



    # PGvector 向量存储（复用已有 PostgreSQL）
    vectorstore:
      pgvector:
        dimensions: 512
        index-type: hnsw
        distance-type: cosine_distance

storage:
  minio:
    endpoint: ${MINIO_ENDPOINT:http://localhost:9000}
    access-key: ${MINIO_ACCESS_KEY:minioadmin}
    secret-key: ${MINIO_SECRET_KEY:minioadmin}
    bucket: ${MINIO_BUCKET:argus-rag-documents}

rag:
  auth:
    jwt-secret: ${DD_RAG_JWT_SECRET:change-me-to-a-32byte-secret-key}
    issuer: argus-rag
    access-token-expire-minutes: 30
    refresh-token-expire-days: 14
    # 仅开发环境兜底，生产环境仍应显式配置安全密钥。
    #    jwt-secret: ${DD_RAG_JWT_SECRET:argus_rag-dev-jwt-secret-2026-04-20-32bytes}
    refresh-cookie-name: ARGUS_DD_RAG_REFRESH_TOKEN
    refresh-cookie-secure: false
  dev-admin:
    username: ${DD_RAG_DEV_ADMIN_USERNAME:your-admin-username}
    email: ${DD_RAG_DEV_ADMIN_EMAIL:your-admin-email@example.com}
    display-name: ${DD_RAG_DEV_ADMIN_DISPLAY_NAME:your-display-name}
    password: ${DD_RAG_DEV_ADMIN_PASSWORD:your-admin-password}
    user-code: ${DD_RAG_DEV_ADMIN_USER_CODE:your-admin-code}

elasticsearch:
  host: ${ES_HOST:localhost}
  port: ${ES_PORT:9200}
  scheme: ${ES_SCHEME:http}
  index-name: dd_rag_document_chunks

ingestion:
  chunking:
    target-tokens: 240
    max-tokens: 320
    overlap-tokens: 32
  vector:
    # 控制业务侧每次调用 VectorStore.add 的 chunk 数量，降低单次 Ollama embedding 请求压力。
    add-batch-size: 9
